<canvas id="@this.Id"
        class="@this.CssClass"
        width="@this.Width"
        height="@this.Height"
        @onmousemove="@this.OnMouseMove"
        @onmouseover="@this.OnMouseOver"
        @ref="this.canvasRef"></canvas>